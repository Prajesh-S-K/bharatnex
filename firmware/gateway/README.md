# ESP32-S3 gateway firmware

Receive contract-compatible node packets, track heartbeat/reconnect state, forward readings to FastAPI and handle buzzer commands. Do not perform the laptop's ML/risk calculations here.

