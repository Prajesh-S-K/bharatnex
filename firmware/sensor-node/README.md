# ESP32 sensor-node firmware

One codebase must support Node A and Node B through configuration. It reads MPU6050 and potentiometer inputs, controls status LEDs, reports health and emits the v1 sensor-reading contract.

