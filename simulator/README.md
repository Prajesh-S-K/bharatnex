# Sensor and failure simulator

Generate deterministic Node A/B packets using the shared contract. The simulator is both the first integration source and the permanent demo fallback.

Required scenarios are listed in `docs/testing/acceptance-tests.md`.

