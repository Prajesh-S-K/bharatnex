# Data

- `geojson/`: local mine-panel, node and active-face geometry for the prototype.
- `samples/`: small, non-sensitive sample datasets.

Do not claim that samples are official CMPDI/CIL data unless their provenance and permission are documented.

